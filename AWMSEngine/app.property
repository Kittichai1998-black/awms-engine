logger.rootpath = D:/logs/{MachineName}/{Date}/
logger.filename = {RefID}.{Date}.log

cronex.311=0 0/10 * * * ?
cronex.321=0 0/10 * * * ?

database.mssql.connection = Server=localhost\SQLEXPRESS;Uid=sa;PASSWORD=@mwte@mp@55;database=AWMS_THIP;Max Pool Size=400;Connect Timeout=600;